rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WatchTracker hanya diakses via Firebase Admin SDK (backend repo ini).
    // Tidak ada client yang boleh akses langsung.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
